<%= form_for @tagging, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :screencast_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :screencast_id, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :tag_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :tag_id, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                taggings_path, :class => 'btn' %>
  </div>
<% end %>
